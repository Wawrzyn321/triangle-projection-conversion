name: Triangle Conversion
on:
  push:
    branches: [ master ]

jobs:
  Units:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/units

  # Deploy:
  #   runs-on: ubuntu-latest
  #   needs: [Units]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: ./.github/actions/deploy
